#include <bits/stdc++.h>
using namespace std;

int main() {
    string s = "Striver";
    
    // The .size() method is used to find the length of a string
    int len = s.size();
    
    cout << s << endl;
    cout << "Length: " << len << endl;

    return 0;
}
