#include <iostream>
/*
include <bits/stdc++.h> makes sure that all libraries are included which is
normally fine, although it takes slightly more time than normal
*/

using namespace std;
int main(){
    int x, y;
    cin >> x >> y;
    cout << "Value of x: " << x << " and y: " << y;
/*
From the above line we can see that the input system can be modified
so that multiple inputs can be taken from one single line of code
*/
    return 0;
}
